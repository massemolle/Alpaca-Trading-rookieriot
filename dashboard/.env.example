# Same Supabase project as ../db.py, read via direct Postgres server-side
# only (never exposed to the browser) — see src/lib/db.ts for why.
SUPABASE_DB_HOST=
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=
SUPABASE_DB_PASSWORD=
SUPABASE_SCHEMA=alpaca_hackathon
